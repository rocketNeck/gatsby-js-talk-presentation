import React from 'react';
import Link from 'gatsby-link';

export default ({ transition }) => (
  <div style={transition && transition.style}>
    <h3>All of these and more are provided out of the box in order provide<br/> a faster User Preceived Performance and<br/> Time To Interaction</h3>
  </div>
);